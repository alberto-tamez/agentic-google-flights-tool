[build-system]
requires = ["hatchling>=1.27"]
build-backend = "hatchling.build"

[project]
name = "agentic-google-flights-tool"
version = "0.1.0"
description = "Explore and compare flight options with AI agents"
readme = "README.md"
requires-python = ">=3.11"
license = "MIT"
license-files = ["LICENSE"]
authors = [{name = "Alberto Tamez Gonzalez"}]
keywords = ["flights", "travel", "google-flights", "browser-automation"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Environment :: Console",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3 :: Only",
    "Programming Language :: Python :: 3.11",
    "Topic :: Internet :: WWW/HTTP :: Browsers",
]
dependencies = [
    "fast-flights==3.1.0",
    "playwright>=1.62,<2",
    "pydantic>=2.10,<3",
]

[project.optional-dependencies]
fli = ["flights==0.9.0"]
dev = [
    "pytest>=8.3,<9",
    "ruff>=0.8",
]

[project.urls]
Homepage = "https://github.com/alberto-tamez/agentic-google-flights-tool"
Repository = "https://github.com/alberto-tamez/agentic-google-flights-tool"
Issues = "https://github.com/alberto-tamez/agentic-google-flights-tool/issues"

[project.scripts]
agentic-flights = "reverse_google_flights.cli:main"
advanced-google-flights-tool = "reverse_google_flights.cli:main"
reverse-google-flights = "reverse_google_flights.cli:main"

[tool.hatch.build.targets.wheel]
packages = ["src/reverse_google_flights"]

[tool.pytest.ini_options]
addopts = "-q"
testpaths = ["tests"]
markers = ["live: calls the live reverse-engineered Google Flights endpoint"]

[tool.ruff]
target-version = "py311"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]

[tool.hatch.build.targets.wheel.force-include]
"docs/agent-guide.md" = "reverse_google_flights/_docs/agent-guide.md"
"docs/reference.md" = "reverse_google_flights/_docs/reference.md"
